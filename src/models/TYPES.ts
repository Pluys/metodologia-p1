export enum RISK {
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
}

export enum TRANSACTION_TYPE {
  BUY = "buy",
  SELL = "sell",
}

export enum TRANSACTION_STATUS {
  PENDING = "pending",
  COMPLETED = "completed",
  FAILED = "failed",
}

export enum ORDER_STATUS {
  PENDING = "pending",
  EXECUTED = "executed",
  CANCELLED = "cancelled",
}
